// Flags de produto.
//
// AI_ENABLED: liga/desliga toda a camada de IA (PITWALL AI — chat flutuante +
// resumo de sessão). Por decisão do produto (2026-06) está OCULTO por enquanto,
// mas o código fica pronto: basta voltar para `true` para reexibir tudo.
export const AI_ENABLED = false
